<view class="container">
    <image src="{{banner.img}}" class="host-img"></image>
    <view class="inner-container">
        <image src="{{left.img}}" class="left"></image>
        <view class="inner-right">
            <image src="{{rightTop.img}}" class="rightTop"></image>
            <image src="{{rightBottom.img}}" class="rightBottom"></image>
        </view>
    </view>
</view>
